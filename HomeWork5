1. 請解釋throw和throws之間的區別
Ans：
throw在方法中使用，能在自定義的情況下拋出例外
throws在方法宣告時使用，若在該方法不處理，可將此例外拋出交由呼叫此方法的方法來處理例外，若拋到最後沒處理，就會由JVM處理。

2. 請實作一個具有多重catch區塊的運作的程式？
3.  請實作一個需要有finally區塊的範例？並說明為什麼。
4. 請實作一個函數具有二個int參數，印出二數前數除後數結果，並具有捕捉異常的處理，前述內述要寫出二個版本，一版為函數內處理異常，另一版是將異常丟出函數外。
5. 請實作一程式，由鍵盤輸入一整數，
若小於0，則會丟出IntegerLessThanZeroException，並印出“數字小於0之異常”
若大於0，則會丟出IntegerGreatetThanZeroException，並印出“數字大於0之異常”
若=0，則會丟出IntegerEqualZeroException，並印出“數字等於0之異常”
6.請實作一main程式呼叫 函數 boolean checkPrime(int n)，N是由鍵盤輸入(使用BufferedReader), 判斷N為質數則回傳true, 若不是則回傳false，若N小於等於1，則丟出自訂的ParameterOutOfBoundException(catch）， 其它種Exception則由丟出函數外。

7.實作一個存儲學生資訊的 Map，每個學生有學號和姓名，至少有三位學生，並輸出所有學生的資訊。
8. 實作一個動態陣列（ArrayList）來存儲整數，當鍵盤輸入整數時可加入ArrayList，當輸入非整數則throw自訂的異常(ParameterOutOfBoundException)，當輸入print時，則會將ArrayList的整數逐個印出。
9. 呈上當輸入treeSet時，則會將ArrayList轉換成TreeSet再依TreeSet的特性印出不重覆且有排序的數字。
10.  實作一個HashSet 來存儲字串，當鍵盤輸入任何字串時(可能會重覆輸入)，都會加入HashSet，當輸入print時，則會將HashSet印出不重覆的各個字串。 

