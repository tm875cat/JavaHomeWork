import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

public class PetsService {
    private List<String> headerList;
    private void initHearderInfo(String header){
        if(this.headerList == null){
            String[] strs = header.split(",");
            this.headerList = new ArrayList<String>(Arrays.asList(strs));
        }
    }

    public List<Object> readPetsFromFile(String fileName){
        return null;
    }

    private Object parsePet(String data){
        String[] str = data.split(",");
        //1. check the first field to know which class we should use
        //2. create an object from the first field
        //3. set field value from data via set methods
        //3.1 get header info
        //3.2 generate method (function) name from header information
        //3.3 get the associated field by header name, which should be the same as field name, and identify the field type
        //3.4 convert data into specific type. in this case, we have to deal with int and double
        //3.5 get declared method by method name
        //3.6 invoke the associate method with converted value


        return null;
    }


}
